package com.app.foodycookbook.utills;

import android.widget.CompoundButton;

public interface CheckBoxOnCheckChangeListener {
    void onCheckedChanged(CompoundButton buttonView, boolean isChecked, int position);
}
